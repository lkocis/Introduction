/*export function handleNextClick(setIndex, authors)
{
    setIndex((prevIndex) => (prevIndex + 1) % authors.length); 
}

export function handlePreviousClick(setIndex, authors)
{
    setIndex((prevIndex) => (prevIndex - 1 + authors.length) % authors.length); 
}

export function handleMoreClick(setShowMore)
{
    setShowMore((prevShowMore) => !prevShowMore);
}*/
